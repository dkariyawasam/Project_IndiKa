# Kinkabuto front sprite

Adapted the supplied Kinkabuto artwork with the built-in image generation tool, then encoded it as a 64×64 indexed GBA sprite. Index zero is transparent; fifteen opaque colours use warm fossil-like gold/brown ramps, ivory greys, navy trim and red eyes. Main ROM build passed; decoded 4bpp indices match the source PNG at every pixel. No live emulator check was performed for this asset.

Front and normal palette replace the placeholders. The shiny front currently uses the same palette, avoiding an unrelated placeholder palette. Back and menu artwork remain pending.

Generation prompt: Adapt the supplied Kinkabuto to a FireRed/LeafGreen front battle sprite; preserve the tall two-pronged kabuto crest, skull mask, red eyes, navy trim, two curved blades, feet and frontal pose. Simplify to a 64×64 grid with a 56×60 subject, fifteen visible colours plus transparency, muted warm fossil golds/browns, ivory greys, restrained navy and red eyes. Crisp outlines and clustered highlights; no gradients, antialiasing, text or ground shadow.

Cleanup prompt: Keep the sprite unchanged and replace the generated checkerboard with uniform RGB(255,0,255), including all gaps. The keyed background becomes transparent palette index zero during GBA encoding.

Revision 2: the first conversion lost facial and blade detail. Regenerated from the original reference using the built-in tool, asking for larger separated red eyes and sockets, a clear black nasal opening, pointed skull cheeks/lower jaw, continuous navy jaw trim, and distinct silver blades/gold rims. Reduced unused canvas space and requested a slightly shorter crest to allocate more space to the face. Encoded to 64×64 with fifteen opaque colours and transparency. Previous front and palette retained here as front-v1.png and normal-v1.pal. Main ROM rebuilt successfully; no live emulator check.

Revision 3: user requested smaller feet to improve eye detail. Built-in image edit prompt: halve the height and width of the two gold feet into tucked claws; use freed lower-body space for a larger skull face and larger red jewel eyes with distinct dark sockets, red shading, pink highlights and ivory glints; preserve the crest, navy jaw trim, armor and side blades. Converted through the same 64×64/16-index pipeline and rebuilt the main ROM. Previous sprite retained as front-v2.png.

Revision 3 reverted at the user's request. Restored front-v2.png as the active front sprite, regenerated the preview and rebuilt the main ROM.

Eye contour revision: built-in edit requested cleaner vertical oval eyes at their existing size, continuous dark socket edges, red shading and small ivory glints. Imported only the two eye regions from the generated result, preserving every pixel elsewhere (verified programmatically). Saved original as front-before-eye-contours.png. Rebuilt main ROM successfully.

Silhouette repair: built-in image edit requested continuous one-pixel dark contours around the crest, armor, arms, blades and feet, preserving open gaps and existing design. Integrated the generated silhouette with original overlapping interior pixels and the existing palette. Applied dark border indices when encoding the outer edge. Verified the opaque sprite forms one four-connected component (2,170 pixels), with no isolated fragments. Previous asset saved as front-before-silhouette.png. Main ROM rebuild passed; live appearance not yet tested.

Face-first restart from the newly supplied original reference: built-in image prompt prioritised small oval pink-red eyes, separate nasal lobes, curved grey cheek seams, ivory cheek points and a distinct lower crescent jaw separated by navy W trim. Requested original proportions and pose, with four grey/ivory palette shades reserved for facial modelling. Generated source saved as generated-face-preserved.png; converted candidate retained in face-candidate/. Updated front.png and normal.pal, then rebuilt main ROM. All 4,096 encoded pixel indices match the source. Prior active sprite/palette saved as front-before-face-restart.png and normal-before-face-restart.pal. No live emulator test for this revision.

Mouth and border refinement: built-in edit requested a continuous navy W-shaped mouth separating upper skull tips from lower ivory crescent jaw, and clean one-pixel dark borders. Imported only the mouth area from the generated edit, retaining the existing skull and eyes. Used the original silhouette with consistent dark edge indices. Eye/nose region verified unchanged; all 4bpp pixels match the indexed source. Main ROM build passed. Previous sprite retained as front-before-mouth-borders.png; generated reference saved as generated-mouth-borders.png.

Compact user reference replacement: directly converted the new supplied artwork (user-compact-reference.png), without generative redrawing. Cropped its 334×360 artwork bounds, nearest-neighbor sampled to 56×60 and centred on 64×64. Exterior-connected white background becomes transparent; interior highlights remain. Encoded using the current fifteen opaque palette colours. Source/design preserved through format conversion, subject to size and palette constraints. Previous front saved as front-before-new-reference.png. All 4bpp indices verified against PNG; main ROM build passed. No live emulator check.

Sharpness-reference pass: used the supplied 97×101 reference directly (art bounds 82×89), sampled without smoothing to 57×62 on the 64×64 canvas. Reserved distinct red/pink eye and cool grey facial palette entries, with dark outline indices along the silhouette. The generated alternative changed facial design and was not installed. Earlier active front and palette retained as front-before-sharpness.png and normal-before-sharpness.pal. Main ROM build passed. The larger reference has more pixels than the GBA sprite, so this is a constrained adaptation rather than exact detail preservation.

Sharpness pass reverted following user feedback that the previous version was cleaner. Restored front-before-sharpness.png and normal-before-sharpness.pal byte-for-byte, regenerated preview, and rebuilt main ROM successfully.

User native replacement (kink.png.png): source is already 64×64 with fourteen colours, so imported directly without resizing, redrawing or colour reduction. Only exterior-connected white background became transparent index zero; internal white details retained. Verified all foreground RGB pixels match supplied source exactly in the indexed PNG. Palette conversion to the GBA's 5-bit channels occurs normally during compilation. Prior sprite/palette backed up as front-before-user-native.png and normal-before-user-native.pal. User source archived as user-native-source.png. Main ROM build passed.

Updated native source supplied after Amunyte: imported current kink.png.png directly at 64×64, preserving all foreground source colours and pixels, with exterior white background transparent. Archived source as user-native-source-update.png; previous assets retained as front-before-native-update.png and normal-before-native-update.pal. Main ROM rebuild passed.

FRLG palette / edge cleanup: retained the native user sprite geometry and interior pixel indices. Inspection found a single connected sprite and no exterior white pixels; light outline interruptions were the likely artifacts. Darkened 37 light perimeter pixels into the appropriate dark brown/charcoal ramp, keeping the entire transparency mask unchanged. Palette uses Kabutops golds/greys, Omastar pale ivory, subdued navy and red. Built-in edit supplied a visual direction but its redrawn shape was not installed. Previous source/palette retained as front-before-frlg-cleanup.png and normal-before-frlg-cleanup.pal. Main ROM rebuild passed; live emulator appearance not checked.
